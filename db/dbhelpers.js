var db = require('./');

module.exports = {
  students: {
    // fetch all students
    getStudent: () => {},
    // add a student
    postName: (student) => {},
    // update a student's name
    updateName: (_id, name) => {},
  }
};